# Copy to .env.local (gitignored) for local builds.
# Never commit real keys.

# Tone3000 publishable key from https://www.tone3000.com/api
# Used at CMake configure time via env Namplifier_TONE3000_KEY
Namplifier_TONE3000_KEY=
